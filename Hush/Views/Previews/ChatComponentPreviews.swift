#if DEBUG

    import SwiftUI

    #Preview("TypingIndicator") {
        TypingIndicator()
            .padding()
    }

    #Preview("ChatConfigPopover") {
        ChatConfigPopover(parameters: .constant(.standard))
    }

#endif
