#if DEBUG

    import SwiftUI

    #Preview("TypingIndicator") {
        TypingIndicator()
            .padding()
    }

    #Preview("ChatConfigDrawer") {
        ChatConfigDrawer(parameters: .constant(.standard))
    }

#endif
